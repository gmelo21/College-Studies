const Button=()=>{
    const handleClick=()=>{
        alert("These are some Elden Ring references.")

    }

    return(
        <>
        <button onClick={handleClick}>Click here</button>
        </>

    )
    
}

export default Button